# F7.4 — Empacotamento e portabilidade

> **Gate:** `COMPLETED_LOCAL / PROMOTION_PENDING`
> **Lifecycle:** `COMPLETED_LOCAL`
> **Autorização vigente:** implementação, certificação e checkpoint locais; promoção remota não autorizada
> **Branch:** `task/f7.4-packaging-portability`
> **Baseline Git:** `7b4dd3d0a8720e6d47b6840cad8e5361fd4f0201` (`main == origin/main`)
> **Checkpoint READY:** `checkpoint/f7.4-ready` → `be777aeb79fbc3dd3eb604d49ec4de8cbc9ffbe4`, somente local
> **Executor:** `Codex`, único escritor autorizado
> **Autorizado em:** `2026-08-29T23:17:02-03:00`
> **Runtime:** `C:\Users\walla\OneDrive\Desktop\ai-engineering-harness\.venv\Scripts\python.exe` — Python 3.12.13
> **uv de smoke local:** `C:\Users\walla\.codex\visualizations\2026\08\11\019fef78-71a6-7681-ae60-156fa23ebfb7\f37-smoke-tools-20260812\Scripts\uv.exe`

## Dependência terminal comprovada

A revisão transversal foi incorporada pelo PR #89 no merge
`7b4dd3d0a8720e6d47b6840cad8e5361fd4f0201`. A CI do PR `33286911120` e a CI de `push`
`33287246294` passaram 11/11 jobs mais `CI required`; a segunda certificou o SHA exato de `main`.
Antes da branch, `main == origin/main`, a árvore estava limpa e não havia dossiê ativo. A branch
remota documental foi preservada e `origin` foi alinhado a `https://github.com/Wf-ops1/Hartrol.git`.

## Problema comprovado

O probe de preparação terminou com exit code `1` e observou:

```text
src/ai_engineering_harness/cli/main.py:208: defaults_dir = Path(__file__).resolve().parent.parent / "defaults"
tests/ci/smoke_wheel.py:82: cwd=ROOT
missing_release_documents=LICENSE,CHANGELOG.md,SUPPORT.md
```

Assim, `harness init` ainda deriva defaults de layout físico, o smoke isolado executa dentro do
checkout e os três documentos de release exigidos não existem. Em contraste, `pyproject.toml` já
declara package data, `.gitignore` já exclui `*.egg-info`, `__pycache__` e `.pytest_cache`, e nenhum
desses artefatos está rastreado.

O baseline real de portabilidade em Windows passou `41 passed, 2 skipped in 33.53s` para
`test_packaging.py`, `test_git_worktree.py` e `test_terminal_adapter.py`. Os dois skips são
exclusivamente POSIX e permanecem cobertos pela matriz Ubuntu. Logo, worktree e terminação de árvore
de processos já têm efeitos reais; a F7.4 deve preservá-los e tornar sua execução Windows/Linux
explicitamente auditável, não reimplementá-los.

## Baseline conhecido

- `main == origin/main == 7b4dd3d0a8720e6d47b6840cad8e5361fd4f0201` antes da branch;
- CI de `push` `33287246294` verde no SHA exato, com 11/11 jobs mais `CI required`;
- branch exclusiva local, sem upstream, criada de árvore limpa;
- runtime Python 3.12.13 e `uv` local exato registrados no cabeçalho;
- `41 passed, 2 skipped` no foco packaging/worktree/terminal; skips exclusivamente POSIX;
- três lacunas negativas reproduzidas e nenhum arquivo de implementação alterado.

## Decisão legal encerrada

O plano exige adicionar uma licença, mas o repositório não contém decisão sobre os direitos de uso e
redistribuição. O executor recomendou Apache-2.0 por ser permissiva e incluir concessão explícita de
patentes, apresentou MIT e termos proprietários como alternativas e pediu uma escolha nominal. Em
`2026-08-29T23:36:44-03:00`, o usuário respondeu `autorizo continue`; como resposta direta à escolha
proposta, a autorização foi registrada como aceite da recomendação Apache-2.0. A implementação deverá
usar o texto oficial integral e metadata coerente, sem alterar autoria, versão ou política de release.

## Contrato técnico congelado

1. `harness init` obterá os defaults exclusivamente pelo pacote instalado com `importlib.resources`,
   sem derivar o diretório de `__file__` e sem sobrescrever arquivos existentes do usuário.
2. A wheel conterá os defaults declarados, o texto da licença e metadata coerente; não conterá
   bytecode, caches ou metadata de build indevida.
3. O smoke receberá a wheel explicitamente, instalará com `uv` isolado e executará versão, leitura de
   recursos e `harness init` em diretório temporário externo ao checkout.
4. O smoke provará que origem importada, diretório de trabalho e scaffold não dependem do checkout nem
   de `src/ai_engineering_harness` no alvo externo.
5. Windows e Linux continuarão certificados pela matriz de CI. Paths e instalação para macOS serão
   documentados como contrato suportado, sem alegar certificação de CI nesta tarefa.
6. Worktree e terminação de árvore de processos permanecerão implementações F7.1/F5.7 existentes;
   seus testes comportamentais serão reexecutados sem alteração de produto.
7. Apache-2.0, changelog, política de suporte e guia de portabilidade serão documentos públicos da
   distribuição. Esta tarefa não publica release, tag ou pacote em registry.

## Escopo congelado

### Implementação e empacotamento permitidos

- `src/ai_engineering_harness/cli/main.py`: trocar somente a descoberta/cópia de defaults por
  `importlib.resources`, preservando cópia sem sobrescrita e sem wiring F7.C1;
- `pyproject.toml`: metadata de licença e URLs para changelog/suporte, sem alterar versão,
  dependências ou intervalo de Python;
- `tests/ci/smoke_wheel.py`: aceitar wheel explícita, resolver `uv` de modo portátil, executar em
  diretório externo e provar recursos/scaffold instalados e higiene do artefato;
- `tests/unit/test_packaging.py` e `tests/unit/test_ci_workflow.py`: contratos de recursos, metadata,
  artefatos e matriz Windows/Linux;
- `LICENSE`, `CHANGELOG.md`, `SUPPORT.md` e novo guia `docs/portability.md`;
- `.gitignore` somente se um probe reproduzível revelar artefato gerado ainda não coberto.

### Estado e documentação permitidos

- `README.md`, `TASK.md`, `docs/tasks/README.md`, este dossiê;
- `docs/user_guide.md`, `tests/unit/test_documentation.py` e `tests/unit/test_task_ledger.py` somente
  para refletir/validar instalação, suporte e paths reais;
- `.secrets.baseline` somente para deslocamentos mecânicos de achados revisados causados pelos testes,
  sem mudar detector, hash ou classificação.

### Reuso obrigatório sem edição

- `ExternalWorktreeManager`, `TerminalAdapter` e seus testes comportamentais atuais;
- matriz F7.2 e gates F7.3, inclusive `CI required`, thresholds e scanners;
- package defaults, schemas, graphs, policies e interfaces públicas além da cópia de recursos no CLI.

### Fora de escopo e proibido

- composição operacional, provider/tools registry, trust/grants/aprovação ou qualquer item F7.C1;
- bump de versão, tag, publicação de release ou distribuição em registry;
- nova dependência, mudança em `uv.lock`, schema, defaults, comportamento Git ou terminação;
- relaxar testes, cobertura, scan, auditoria de dependências ou proteção de branch;
- alegar suporte macOS validado por CI; macOS será documentado como path/contrato não certificado.

## Critérios de aceite congelados

1. O probe negativo acima retorna zero: os documentos existem e os padrões `Path(__file__)` para
   defaults e `cwd=ROOT` no smoke não existem.
2. `.venv\Scripts\python.exe -m pytest tests/unit/test_packaging.py tests/unit/test_git_worktree.py
   tests/unit/test_terminal_adapter.py tests/unit/test_ci_workflow.py tests/unit/test_documentation.py
   tests/unit/test_task_ledger.py -q` passa, preservando skips exclusivamente POSIX no Windows.
3. `.venv\Scripts\python.exe -m compileall -q src compiler tests`, Ruff e
   `.venv\Scripts\python.exe -m mypy --strict src` passam.
4. A wheel construída contém defaults, metadata/licença e nenhum `egg-info`, `__pycache__`,
   `.pytest_cache`, `.pyc` ou `.pyo`; o smoke instala via `uv` e executa `harness init` em diretório
   externo sem `src/ai_engineering_harness`.
5. `tests/run_f7_2_matrix.py`, testes unitários/E2E integrais e gates F7.3 permanecem verdes.
6. A CI do PR passa quality/tests/package em Windows e Linux, segurança/cobertura e `CI required`.
7. `git diff --check`, auditoria de escopo, links/encoding e ausência de artefatos rastreados passam.

## Checklist de liberação

- [x] baseline Git, branch, runtime e CI anterior observados;
- [x] escopo de implementação/documentação e fora de escopo congelados;
- [x] rollback com gatilhos, procedimento e verificação definido;
- [x] checkpoint `checkpoint/f7.4-ready` existe somente localmente antes da implementação;
- [x] implementação e documentação permanecem no allowlist congelado;
- [x] todos os critérios de aceite locais possuem evidência positiva no head candidato;
- [x] nenhuma ref remota, PR, merge, tag ou release foi criada;
- [x] nenhuma evidência negativa de produto permaneceu aberta antes do checkpoint local.

## Evidência de implementação e certificação local

- o CLI passou a ler os defaults por `importlib.resources` e preserva arquivos do usuário; a wheel
  contém defaults e `LICENSE`, declara `License-Expression: Apache-2.0` e não contém cache,
  bytecode ou `egg-info`;
- o smoke instalou a wheel explícita com `uv` isolado e executou versão, metadata, recursos e
  `harness init` fora do checkout, preservando o sentinel do usuário;
- SHA-256 da wheel certificada:
  `83E389C7592486E09B0A35B7B08636D8ADA481C22130E2E6A62A68FFC1DBEDBA`;
- foco de empacotamento: `41 passed`; aceite combinado: `116 passed, 2 skipped`; documentação,
  encoding e contratos de CI: `44 passed, 6 subtests passed`; matriz F7.2: `62 passed`;
- suíte integral: `1097 passed, 5 skipped, 6 subtests passed in 698.29s`; recertificação com
  coverage: `1097 passed, 5 skipped, 6 subtests passed in 1022.24s`, core `88.73%`, 23 funções
  decisórias e zero ramo ausente;
- Ruff, mypy strict sobre 110 arquivos, compileall, `uv lock --check`, `git diff --check` e ausência
  de artefato gerado rastreado passaram;
- auditoria: 74 dependências, zero ignorada e zero vulnerabilidade; baseline de secrets preserva
  307 achados revisados e o scan diferencial dos 13 arquivos de produto/documentação encontrou zero
  achado novo. O wrapper local do `detect-secrets` mantém a limitação conhecida de serializar paths
  com `\\` no Windows; a CI Ubuntu continua sendo a autoridade de promoção para esse gate;
- a construção e a auditoria de dependências precisaram de rede fora do sandbox, sem alterar
  dependências runtime nem `uv.lock`; nenhum efeito remoto Git foi produzido.

## Rollback

- **Gatilhos:** default ausente/diferente após instalação; overwrite de arquivo do usuário; wheel
  importando o checkout; artifact gerado rastreado; regressão Windows/Linux; metadata legal divergente;
  mudança de wiring F7.C1; qualquer gate F7.2/F7.3 negativo.
- **Procedimento:** interromper, registrar `REPAIR_ACTIVE / PROMOTION_BLOCKED` e reverter por commit
  isolado ou inversão explícita dos hunks da F7.4; nunca usar `git reset --hard` nem apagar trabalho
  preexistente. Após merge, usar revert do merge e registrar `POST_PROMOTION_BLOCKED`.
- **Verificação:** repetir o aceite integral aplicável e comprovar `git status --short`, imports,
  CLI fail-closed, branch/base e CI no SHA observado.

## Próxima ação

Consolidar o commit candidato e criar `checkpoint/f7.4-complete` somente locais. Parar antes de push,
PR, merge, tag remota ou F7.C1 e exigir autorização nominal para a promoção.
