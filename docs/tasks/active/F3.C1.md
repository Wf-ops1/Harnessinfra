# F3.C1 — Integridade de modelo e model-turn

> **Gate:** `READY`
> **Lifecycle:** `COMPLETED_LOCAL / PROMOTION_PENDING`
> **Executor:** `Codex`, único escritor
> **Autorizado em:** `2026-08-07T23:48:49-03:00`
> **Runtime:** `C:\Users\walla\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe` — Python 3.12.13
> **uv:** `.\build\f0.6-tools\uv\bin\uv.exe` — 0.11.32
> **Fronteira:** somente F3.C1; efeitos duráveis/policy F3.C2 e path/terminal/worktree/edição F3.4–F3.8 permanecem bloqueados.

## Problema comprovado

| Evidência reproduzível | Resultado observado |
|---|---|
| `rg -n "tool_loop_transcript" src/ai_engineering_harness/runtime/tool_loop.py` | a segunda rodada é outro prompt textual, sem `function_call_output`, `role=tool` ou `tool_call_id` nativos |
| inspeção de `BaseLLMProvider.call_tools` e `OpenAICompatibleHTTPProvider.call_tools` | contrato aceita somente `prompt: str`; não há continuação tipada ou reconstrução nativa de turnos |
| reprodução com `LLMResponse(prompt_tokens=100, completion_tokens=100, total_tokens=0)` e `BudgetTracker(max_tokens=1)` | resposta aceita; budget consome `0` e permanece disponível |
| reprodução de `_parse_tool_arguments('{"x":NaN}')` | parser aceita `{'x': nan}` embora não seja JSON interoperável |
| inspeção de `ToolLoopResult`, `ToolLoopError` e `NodeExecutionResult` | sucesso guarda somente o último `ModelCallMetadata`; erro guarda apenas tools; turnos anteriores se perdem |
| inspeção de `_append_node_event`/replay | outcome aceita somente um conjunto plano de campos `model_*`; não representa todos os calls do node |
| auditoria pós-merge da F3.3 | suíte anterior (`508 passed, 1 skipped`) não inspecionava o corpo HTTP da segunda rodada nem usage inconsistente |

Essas lacunas são anteriores a F3.4, mas afetam budget, auditabilidade, fallback e a capacidade dos
providers reais de continuar uma tool call. A correção é necessária antes de habilitar qualquer
ferramenta operacional com efeitos de arquivo, processo ou Git.

## Baseline conhecido

```yaml
baseline:
  branch: "task/f3.c1-model-turn-integrity"
  head: "0e64a88fbe1ca28b8da6a4598a4f4391ba916dd1"
  status: "clean antes da criação do gate; nenhuma mudança preexistente do usuário"
  main: "0e64a88fbe1ca28b8da6a4598a4f4391ba916dd1"
  origin_main: "0e64a88fbe1ca28b8da6a4598a4f4391ba916dd1 após fetch"
  upstream_ci:
    pr_22_run: "31232616249; pull_request; 11/11 completed/success"
    post_merge_run: "31232731611; push; main; head 0e64a88; 11/11 completed/success"
  previous_promotion: "F3.3 certificada e arquivada neste primeiro commit"
  checkpoint: "checkpoint/f3.c1-ready no primeiro commit documental desta branch"
  python: "3.12.13"
  uv: "0.11.32"
```

## Contrato técnico congelado

1. O primeiro model call continua usando `call_tools(prompt, schemas)`. Toda rodada posterior usará
   um contrato imutável e provider-neutral contendo prompt inicial, respostas normalizadas e resultados
   JSON-native ligados por `call_id`; nenhum transcript XML/textual será criado.
2. O provider Responses reconstruirá input nativo com mensagem inicial, itens `function_call` e
   `function_call_output`. O provider Chat Completions reconstruirá mensagens `assistant` com
   `tool_calls` e mensagens `role=tool` com `tool_call_id`. Schemas continuam presentes em cada rodada.
3. Fallback transitório pode reconstruir a mesma conversa em outro provider autorizado; prompt,
   resultados e IDs não serão persistidos pelo contrato de continuação nem convertidos em texto livre.
4. `LLMResponse` e `ModelCallMetadata` exigirão `total_tokens == prompt_tokens + completion_tokens`.
   Inconsistência falha como resposta inválida antes de budget, retorno ou próximo efeito.
5. Todo JSON vindo do provider será estrito: constantes não finitas e chaves duplicadas são rejeitadas
   em payload HTTP, structured output e argumentos de tool. Valores JSON-native não finitos também
   falham no contrato tipado.
6. Cancelamento será verificado pelo router antes de cada candidato, após uma falha transitória, após
   uma resposta e antes de fallback, cobrança ou retorno. Nenhuma continuação/fallback começa após o
   token ser cancelado.
7. `ToolLoopResult` preservará, em ordem, metadata de todos os model calls, mantendo acesso compatível
   ao último call e à contagem. `ToolLoopError` carregará calls já concluídos em qualquer falha posterior.
8. `NodeExecutionResult` terá uma sequência canônica de model calls. Helpers legados que recebem um
   único `model_call` continuarão aceitos, mas a representação interna e durável será a sequência.
9. O outcome do node persistirá `model_calls` como lista redigida de provider, modelo, usage e IDs.
   Replay validará ordem, tipos, usage e unicidade de response IDs; continuará aceitando somente o
   formato plano histórico de chamada única, nunca ambos no mesmo evento.
10. Nenhum prompt, conteúdo, argumentos/resultados brutos, header, token ou secret será adicionado ao
    journal. O contrato de conversa existe apenas em memória durante o loop.

### Recongelamento R1 — preservar todos os output items da Responses API

Durante a implementação, a conferência da
[documentação oficial de function calling](https://developers.openai.com/api/docs/guides/function-calling)
confirmou que,
ao gerenciar estado manualmente — necessário porque o provider usa `store: false` — o segundo request
deve reenviar **todos** os itens de `response.output`, não apenas o `function_call`. Isso inclui itens
de reasoning/encrypted state retornados com a tool call. A inspeção ocorreu com hunks ainda não
commitados; a execução foi pausada para registrar este refinamento antes de qualquer validação ou
mudança adicional.

R1 fortalece o critério de continuação nativa já congelado: o estado provider-native fica somente em
memória, é excluído de dumps/evidência durável e é reenviado sem perda apenas ao mesmo provider;
fallback para outro provider ou estilo de API usa a representação normalizada provider-neutral. Não há novo path, dependência,
efeito externo ou fronteira funcional, portanto o checkpoint `checkpoint/f3.c1-ready` permanece
válido e não há ampliação material.

## Escopo congelado

### Permitido

- `TASK.md`, `README.md`, `docs/fase3_realignamento_operacional.md` e
  `docs/plano_implementacao_harness_operacional.md` — alinhamento e estado honesto;
- `docs/tasks/README.md`, `docs/tasks/completed/F3.3.md` e este dossiê — promoção, DEC-012 e evidência;
- `src/ai_engineering_harness/models/provider.py` e `models/__init__.py` — contrato de conversa,
  requests nativos, JSON estrito e usage íntegra;
- `src/ai_engineering_harness/models/router.py` — continuação/fallback/cancelamento/budget;
- `src/ai_engineering_harness/runtime/tool_loop.py` e `runtime/__init__.py` — conversa nativa e
  metadata completa em sucesso/falha;
- `src/ai_engineering_harness/runtime/node_executors.py` — sequência canônica de model calls;
- `src/ai_engineering_harness/runtime/graph_executor.py` — persistência/replay compatível;
- `tests/unit/test_models.py`, `test_model_routing.py`, `test_tool_loop.py`,
  `test_graph_executor.py` e `test_task_ledger.py` — provas focadas e estruturais.

### Proibido

- qualquer outro path, especialmente `.github/`, `pyproject.toml`, `uv.lock`, `compiler/`, contratos
  de grafo, persistence/storage, tools/router, policies, security, workspace e adapters de tool;
- mudar a ordem ou a durabilidade dos eventos de tool, dispatch, deny-wins ou aprovação; isso é F3.C2;
- path guard, terminal, processo filho, worktree, promoção, edição ou qualquer efeito F3.4–F3.8;
- provider/adaptação nova, backend default, dependência, retry de tool ou relaxamento de egress/budget;
- persistir conversa, prompt, conteúdo, tool payload bruto, segredo ou metadata fabricada;
- alterar critérios da Fase 3 ou iniciar F3.C2/F3.4 na mesma branch/PR.

## Critérios de aceite congelados

```yaml
acceptance:
  native_provider_turns:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_models.py -q -k
      "continuation or native or tool_result or strict_json or usage_consistency"
    expected: >-
      Responses e Chat têm primeiro/segundo body exatos; todos os response.output (inclusive reasoning)
      e function_call_output/role=tool presentes; estado nativo ausente de dumps/journal; transcript
      textual ausente; estado nativo não cruza providers; JSON/usage inválidos falham antes de novo efeito
  loop_evidence_and_cancellation:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_tool_loop.py tests/unit/test_model_routing.py -q -k
      "model_call or continuation or cancel or fallback or budget"
    expected: >-
      todos os calls ficam ordenados no sucesso/erro; cancelamento impede fallback/continuação/cobrança
      indevidos; budget usa total consistente
  journal_compatibility:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_graph_executor.py -q -k "model_call"
    expected: >-
      lista de calls íntegra e redigida; replay aceita formato novo e evento histórico singular,
      rejeitando mistura, inconsistência, duplicidade ou adulteração
  compatibility:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_models.py tests/unit/test_model_routing.py tests/unit/test_tool_loop.py
      tests/unit/test_agent_centric.py tests/unit/test_graph_executor.py
      tests/e2e/test_retry_context.py -q
    expected: "providers F3.1, routing F3.2, loop F3.3 e runtime F2 permanecem verdes"
  documentation:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_task_ledger.py tests/unit/test_documentation.py tests/unit/test_encoding.py -q
    expected: "painel curto, dossiê único, DEC-012, links e UTF-8 íntegros"
  regression:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest tests/unit tests/e2e -q
    expected: "suíte integral verde; somente integração live explicitamente condicionada pode skipar"
  quality:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m mypy src;
      .\build\f0.6-tools\uv\bin\uv.exe run python -m ruff check .;
      .\build\f0.6-tools\uv\bin\uv.exe run python -m compileall -q src compiler tests;
      .\build\f0.6-tools\uv\bin\uv.exe lock --check;
      git diff --check
    expected: "todos exit 0; lockfile byte-idêntico"
  package:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m build;
      .\build\f0.6-tools\uv\bin\uv.exe run python tests/ci/smoke_wheel.py
    expected: "sdist/wheel gerados e wheel instalado/inspecionado com sucesso"
  scope:
    command: >-
      git diff --name-only checkpoint/f3.c1-ready;
      git diff --exit-code checkpoint/f3.c1-ready -- .github pyproject.toml uv.lock compiler
      src/ai_engineering_harness/contracts src/ai_engineering_harness/persistence
      src/ai_engineering_harness/security src/ai_engineering_harness/workspace
      src/ai_engineering_harness/tools src/ai_engineering_harness/models/adapters
    expected: "somente o allowlist F3.C1; F3.C2 e F3.4–F3.8 byte-idênticas"
```

Qualquer teste novo que reproduza um requisito congelado soma evidência; não substitui nem reduz os
comandos acima. Falha em Python suportado pelo CI, mesmo não reproduzida no runtime local, bloqueia o PR.

## Rollback

### Gatilhos

- segundo request usar transcript textual ou omitir os vínculos nativos do provider;
- continuação perder call ID, resultado ou schema, ou fallback enviar contrato incompatível;
- usage inconsistente/non-finite/duplicado chegar ao budget ou JSON não estrito ser aceito;
- qualquer model call concluído desaparecer do sucesso, da falha ou do journal;
- replay novo quebrar eventos históricos válidos ou aceitar evento misto/adulterado;
- cancelamento permitir candidato, fallback, cobrança ou efeito posterior;
- prompt/conteúdo/payload bruto/secret aparecer no journal;
- gate focado, regressão, qualidade, package, documentação ou escopo falhar;
- solução exigir qualquer item proibido ou ampliar materialmente o congelado.

### Procedimento não destrutivo

Parar e preservar saídas. Antes de commit, inverter somente hunks F3.C1 com `apply_patch`. Depois de
commit, usar `git revert` nos commits exclusivos desta branch. Nunca usar reset/clean/force-push nem
descartar trabalho alheio. Se a descoberta exigir F3.C2 ou fase futura, registrar `BLOCKED` e
recongelar antes de qualquer edição adicional.

### Verificação pós-rollback

Confirmar status, HEAD/checkpoint, `git diff --check` e zero diff de produto contra
`checkpoint/f3.c1-ready`; repetir documentação, compatibilidade, suíte integral, quality e package.

## Checklist de liberação

```text
[x] problema comprovado por reprodução e inspeção exata de contratos
[x] baseline Git, PR #22 e CI pré/pós-merge registrados
[x] promoção F3.3 certificada e arquivada neste primeiro commit
[x] escopo de conversa nativa, usage, JSON, cancelamento e evidência congelado
[x] efeitos/policy F3.C2 e paths/processos/Git F3.4–F3.8 excluídos
[x] critérios focados, compatibilidade, documentação, regressão, qualidade, package e escopo congelados
[x] rollback não destrutivo, executor, runtime, horário e checkpoint definidos
```

## Evidências de execução

### Implementação observada

| Área | Resultado |
|---|---|
| Continuação nativa | primeira rodada conserva `call_tools`; rodadas seguintes usam conversa tipada e imutável, sem transcript textual |
| Responses | todos os itens de `response.output`, inclusive reasoning/encrypted state, retornam somente ao mesmo provider antes de `function_call_output`; fallback usa calls normalizadas |
| Chat Completions | segunda rodada contém `assistant.tool_calls` e mensagens `role=tool` vinculadas por `tool_call_id`; schemas permanecem presentes |
| JSON e usage | payload HTTP, argumentos e structured output rejeitam não finitos/chaves duplicadas; resposta e metadata exigem soma exata de tokens |
| Router | cancelamento é reavaliado antes de candidato/fallback e após resposta; resposta concluída rejeitada por cancelamento, identidade ou budget preserva metadata sem retorno indevido |
| Loop | todos os model calls concluídos ficam ordenados no sucesso e nas falhas posteriores; acesso ao último call e contagem permanecem compatíveis |
| Journal/replay | outcome persiste lista redigida de calls; replay valida forma, usage, ordem e IDs, aceita singular histórico e rejeita mistura/adulteração |
| Privacidade | estado de continuação, prompt, conteúdo e payload bruto permanecem em memória e ausentes dos dumps/journal |
| Fronteira | nenhum dispatch/policy/durabilidade F3.C2, adapter, dependência, workflow ou efeito F3.4–F3.8 foi alterado |

### Validações executadas

| Critério | Resultado observado |
|---|---|
| Gate documental | commit `2ecadf9`; tag local `checkpoint/f3.c1-ready`; implementação começou somente após `READY` |
| Continuação nativa | `11 passed, 22 deselected` |
| Loop/cancelamento/budget | `14 passed, 17 deselected` |
| Journal/replay | `3 passed, 36 deselected` |
| Compatibilidade | `117 passed` em F2 e F3.1–F3.3 |
| Documentação | `15 passed, 6 subtests passed` |
| Suíte integral | `527 passed, 1 skipped, 6 subtests passed`; único skip é a integração live explicitamente condicionada |
| mypy | sem issues em 99 arquivos; somente duas notas preexistentes do indexer |
| Ruff | todos os checks verdes |
| compileall | exit 0 para `src`, `compiler` e `tests` |
| Lock | `uv lock --check` exit 0; 41 packages; lock byte-idêntico |
| Package | sdist/wheel gerados; smoke isolado confirmou metadata/package `0.1.0`, CLI e origem fora do workspace |
| Diff | `git diff --check` exit 0 |
| Escopo | somente o allowlist F3.C1; paths proibidos byte-idênticos ao `checkpoint/f3.c1-ready` |

O primeiro smoke local encontrou `WinError 2` porque o subprocesso exige `uv` no `PATH`, enquanto
esta sessão expõe apenas o caminho absoluto do binário versionado no projeto. O mesmo comando foi
repetido com `build/f0.6-tools/uv/bin` no `PATH` somente do processo e ficou verde, reproduzindo o
ambiente do CI. Nenhum arquivo, critério, teste ou configuração foi alterado para obter o resultado.

### Estado de liberação local

A implementação e todos os critérios congelados estão verdes.

### Evidência remota observada após publicação

| Evidência | Resultado observado |
|---|---|
| Observado em | `2026-08-08T01:40:57-03:00` |
| PR | #23, aberto, não draft, base `main`, branch `task/f3.c1-model-turn-integrity` |
| Head publicado | `697cb61db9f628b85df57c6b75e9ed2fb7d1cd05`, igual ao HEAD local e ao upstream no momento da inspeção |
| CI do PR | run `31237686951`; 11/11 checks `completed/success`, incluindo `CI required` e matrizes Linux/Windows |
| Condição pré-merge | `mergeable=true`, `mergeable_state=clean`; nenhum conflito ou check pendente, ausente, ignorado ou falho |
| Efeitos não realizados | nenhum merge, CI pós-merge, sincronização de `main` ou início de F3.C2 foi antecipado |

A interrupção da conversa ocorreu depois da publicação e da conclusão da CI, deixando o painel no
snapshot local anterior ao push. Esta correção documental registra somente fatos remotos observados.
Como ela produz um head documental posterior sem alterar produto, esse head também deve repetir todos
os checks antes do merge; a condição deve ser revalidada ao vivo e não será antecipada neste commit.

### Validação da reconciliação documental

| Critério repetido | Resultado observado |
|---|---|
| Documentação/encoding | `15 passed, 6 subtests passed` |
| Regressão integral | `527 passed, 1 skipped, 6 subtests passed`; apenas integração live condicionada permaneceu skipped |
| Qualidade | mypy sem issues em 99 arquivos; Ruff e compileall verdes |
| Lock | 41 packages resolvidos; `uv lock --check` verde e lockfile inalterado |
| Package | sdist/wheel `0.1.0` gerados; smoke isolado confirmou metadata, CLI e origem fora do workspace |
| Diff e escopo | `git diff --check` verde; reconciliação altera somente `TASK.md` e este dossiê; fronteiras proibidas permanecem byte-idênticas |

O smoke repetiu primeiro o `WinError 2` ambiental já registrado acima e ficou verde quando o diretório
do mesmo binário `uv` versionado foi acrescentado somente ao `PATH` do processo. Nenhum arquivo,
dependência, teste ou critério foi alterado para obter o resultado.

### Commits e checkpoint local

| Commit | Conteúdo |
|---|---|
| `2ecadf9` | certificação da promoção F3.3, DEC-012 e gate F3.C1 completo antes da implementação |
| `8bd0caa` | continuação nativa, JSON/usage, cancelamento, evidência integral, journal/replay e testes |
| `697cb61` | fechamento local, aceite integral e tag `checkpoint/f3.c1-complete` |

O fechamento documental recebeu a tag local `checkpoint/f3.c1-complete`. PR e checks do head publicado
foram observados acima; merge e CI pós-merge continuam pendentes e só serão registrados depois de
observados. A promoção será certificada no primeiro commit do gate F3.C2, após autorização nova, sem
PR recursivo.

## Checklist de conclusão local

```text
[x] Responses preserva todos os output items somente no mesmo provider e envia function_call_output
[x] Chat Completions envia assistant.tool_calls e role=tool vinculados por tool_call_id
[x] fallback entre providers usa representação normalizada, sem transcript textual
[x] JSON e usage inválidos falham antes de budget, retorno ou novo efeito
[x] cancelamento impede fallback/cobrança e preserva metadata de resposta já concluída
[x] todos os model calls permanecem ordenados no sucesso, falhas e journal
[x] replay aceita lista canônica e singular histórico, rejeitando mistura/adulteração
[x] 527 testes + 6 subtests verdes; único skip live condicionado
[x] mypy, Ruff, compileall, lock, package/smoke, diff-check e escopo verdes
[x] F3.C2 e F3.4–F3.8 permanecem fora do diff
[x] PR único publicado e todos os checks do head `697cb61` explicitamente inspecionados
[ ] merge e CI push em main certificados no primeiro commit do gate F3.C2
```
