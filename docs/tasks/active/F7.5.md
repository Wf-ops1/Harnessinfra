# F7.5 — Release candidate do MVP operacional

> **Gate:** `COMPLETED_LOCAL`
> **Lifecycle:** `PROMOTION_PENDING`
> **Autorização vigente:** ciclo integral F7.5, incluindo commits, push, PR, CI, merge, pós-merge, tag e GitHub prerelease; sem remoção de refs e sem iniciar F8
> **Branch:** `task/f7.5-release-candidate`
> **Baseline Git:** `3415c385ae185f240d8707e98fe5d6a8d51e7a98` (`main == origin/main`) <!-- pragma: allowlist secret -->
> **Versão candidata:** package `0.2.0rc1`; tag final `v0.2.0-rc.1`
> **Checkpoint READY:** `checkpoint/f7.5-ready` → `ec2296d68669e5b155dc2ba3f037a5197b5e55b3`, somente local
> **Produto local:** `9f7886a9497aa22cd8e50a8fe8f85591088029ab` <!-- pragma: allowlist secret -->
> **Checkpoint COMPLETE:** pendente sobre o commit documental desta certificação
> **Executor:** `Codex`, único escritor
> **Autorizado em:** `2026-08-30`
> **Gate preparado em:** `2026-08-30T15:21:06-03:00`
> **Runtime:** `C:\Users\walla\OneDrive\Desktop\ai-engineering-harness\.venv\Scripts\python.exe` — Python 3.12.13

## Dependências terminais comprovadas

A F7.4 e a F7.C1 estão promovidas, reconciliadas e verdes em `main`. A cadeia mais recente é:

- produto F7.C1: PR #92, head `bb4dc8c`, CI `33325055342`, merge `26c36ff` e
  pós-merge `33325679613`;
- reconciliação F7.C1: PR #93, head final
  `c99f02e43bdbe2a8763c1cce3cc6e6188cfe99b2`, CI `33326822791`, merge
  `3415c385ae185f240d8707e98fe5d6a8d51e7a98` e pós-merge `33327198301`; <!-- pragma: allowlist secret -->
- o run pós-merge passou onze jobs obrigatórios mais `CI required`, incluindo E2E Windows 3.11/3.14,
  package Windows/Linux e security/coverage;
- não havia tag remota nem GitHub Release antes deste gate; branches e checkpoints anteriores estão
  preservados.

## Problema comprovado

O produto já satisfaz a vertical operacional promovida, mas ainda não existe uma release candidate
publicável e as superfícies correntes divergem desse fato:

1. `pyproject.toml` e `uv.lock` ainda identificam `0.1.0`, versão histórica do protótipo;
2. o driver SARIF de audit repete `0.1.0` em código, fora da fonte autoral única;
3. README, suporte, portabilidade, guia e documentos arquiteturais ainda afirmam que a composição
   pública F7.C1 falta ou que o registry padrão permanece vazio;
4. não existe lista pública consolidada de limitações conhecidas;
5. não há tag, GitHub prerelease ou artefatos publicados.

Rotular retroativamente o histórico como `v0.1.0` misturaria o protótipo antigo com o MVP agora
promovido. A próxima identidade PEP 440 é congelada como `0.2.0rc1`; a tag legível no GitHub será
`v0.2.0-rc.1`. É uma prerelease `0.x`, nunca `1.0` nem declaração de produção.

## Baseline conhecido

- `harness doctor --json --workflow new-feature` retornou exit `1` e `UNHEALTHY`, com falhas
  reais e tipadas para Git/worktree, provider inalcançável e workflow ausente no diretório corrente;
  Python e storage passaram e MCP desabilitado ficou `NOT_APPLICABLE`;
- foco doctor/package/docs/CI executado no sandbox: `48 passed`; o E2E foi inicialmente impedido
  apenas pelo cache global do `uv` bloqueado e foi recertificado sem sandbox pelo mesmo teste:
  `1 passed in 32.97s`;
- a tentativa anterior sem TEMP confinado produziu 16 erros de setup por
  `PermissionError` em `pytest-of-walla`; a evidência ambiental não foi apagada nem tratada como
  defeito de produto;
- Ruff passou; mypy strict passou em 112 arquivos-fonte; compileall passou; `uv lock --check`
  resolveu 75 pacotes sem divergência;
- a busca de claims correntes reproduziu as declarações obsoletas listadas acima;
- a CI terminal `33327198301` é a certificação integral do baseline exato.

## Contrato congelado

1. A fonte autoral do package passa a `0.2.0rc1`; lock, metadata instalada, `__version__`, CLI e
   SARIF devem refletir essa identidade sem criar uma segunda constante.
2. A documentação corrente descreve F7.C1 como composição pública promovida e distingue:
   capacidade operacional do MVP, dependências externas opt-in e limitações da RC.
3. `KNOWN_LIMITATIONS.md` publica as limitações conhecidas e é ligado por README, suporte,
   portabilidade, guia e release notes.
4. Changelog registra `0.2.0rc1` como MVP operacional release candidate e preserva `0.1.0` como
   baseline histórico não publicado.
5. Nenhum comportamento runtime, schema, default, policy, threshold, dependência ou workflow de CI
   muda nesta tarefa além da versão pública e da eliminação da duplicação SARIF.
6. Depois do merge e da CI pós-merge verdes, a tag imutável `v0.2.0-rc.1` apontará para o merge
   certificado e uma GitHub prerelease anexará wheel e sdist construídos desse SHA.
7. Não haverá publicação em PyPI: não existe workflow/credencial/política congelada para registry.

## Escopo congelado

### Produto e metadata

- `pyproject.toml` e atualização mecânica correspondente de `uv.lock`;
- `src/ai_engineering_harness/observability/audit.py` somente para usar `PACKAGE_VERSION` no SARIF;
- testes unitários de packaging, audit/versionamento e contratos documentais.

### Documentação pública

- `README.md`, `CHANGELOG.md`, `SUPPORT.md` e novo `KNOWN_LIMITATIONS.md`;
- `docs/portability.md`, `docs/user_guide.md`, `docs/agentic_operating_model.md`,
  `docs/agentic_lifecycle_audit.md`, `docs/harness_architecture_spec.md`,
  `docs/walkthrough.md` e `docs/walkthrough_audit.md`;
- painel, índice de tarefas e este dossiê.

### Efeitos de release autorizados

- branch, commits, checkpoints locais, push sem force, PR, checks, merge commit e CI pós-merge;
- tag anotada `v0.2.0-rc.1` no merge certificado, push somente dessa tag e GitHub prerelease;
- anexar wheel e sdist construídos do checkout limpo do merge e publicar seus SHA-256 nas notas.

## Fora de escopo

- qualquer item da Fase 8, workflow adicional ou expansão para linguagens além de Python;
- mudança de provider/tool/worktree/lifecycle, schema, defaults, policy, dependência ou quality gate;
- publicação em PyPI/outro registry, assinatura externa, auto-update ou instalador de sistema;
- declarar produção, estabilidade `1.0`, macOS certificado por CI ou autonomia irrestrita;
- mover, apagar ou recriar tag publicada, force-push, bypass de checks ou remoção de branches/refs.

## Limitações obrigatórias a publicar

- somente `new-feature` possui composição pública operacional certificada;
- execução local, em uma máquina, para repositórios Python versionados com Git;
- providers OpenAI/local exigem configuração, serviço e credencial explícitos; Anthropic não existe;
- MCP/Serena continuam opcionais, e memória semântica externa não é requisito da RC;
- Windows e Linux são certificados; macOS tem contrato de path, sem job de CI;
- evidence é tamper-evident local, sem âncora externa imutável;
- rollback Git não reexecuta automaticamente os gates após a reversão;
- interfaces permanecem prerelease, sem promessa de compatibilidade estável;
- distribuição desta RC é GitHub Release, não PyPI;
- “MVP operacional” não significa uso autônomo sem supervisão nem autorização para repositório valioso.

## Critérios de aceite congelados

1. Testes provam `0.2.0rc1` em metadata/package/CLI/SARIF e ausência do literal autoral duplicado.
2. Testes documentais provam status RC, link e conteúdo mínimo de `KNOWN_LIMITATIONS.md`, além da
   ausência das claims correntes obsoletas sobre F7.C1.
3. Doctor dedicado permanece verde e o probe direto unhealthy retorna não zero com falhas reais.
4. E2E público da wheel, matriz F7.2 e suíte integral passam sem skip novo ou relaxamento.
5. Ruff, mypy strict, compileall, diff-check e `uv lock --check` passam.
6. Gates F7.3 passam: core >=80%, decisões 100%, zero secret novo e zero vulnerabilidade auditada.
7. Wheel e sdist limpas são construídas; smoke externo confirma `0.2.0rc1` fora do checkout.
8. PR passa todos os jobs Windows/Linux mais `CI required`; merge e pós-merge repetem a prova.
9. Tag aponta ao merge certificado; GitHub release é `prerelease`, contém wheel/sdist e publica
   checksums; não há release `1.0`, PyPI ou tag movida.

## Certificação local

- package, metadata instalada, CLI e SARIF compartilham `0.2.0rc1`; `uv.lock` mudou somente a versão;
- documentação/package/audit/ledger focados passaram `60/60`; doctor e quality dedicados passaram `33/33`;
- E2E público da wheel passou `1 passed in 31.83s`; matriz F7.2 R2 passou
  `62 passed in 62.23s` após o R1 preservar `61 passed / 1 falha` por `HARNESS_TEST_UV` ausente;
- full/coverage R1 preservou `1107 passed, 1 failed, 5 skipped, 6 subtests in 639.82s`: o único
  negativo foi acesso negado ao `%LOCALAPPDATA%` global pelo sandbox. O caso isolado passou `1/1`
  com path confinado, e a R2 integral passou `1108 passed, 5 skipped, 6 subtests in 666.92s`;
- cobertura crítica `88.73%` em seis arquivos, 23 funções decisórias e zero branch obrigatória
  ausente; Ruff, mypy strict em 112 fontes, compileall, diff-check e `uv lock --check` passaram;
- secrets: `files=348 reviewed_fixtures=307 new_secrets=0`; dependências: <!-- pragma: allowlist secret -->
  `audited=74 skipped_dependencies=0 vulnerabilities=0`;
- build isolado produziu wheel de 392178 bytes, SHA-256
  `0E251F3759CB01CC82818CD5775B611CE35960FE69404A484C47E717FAE097E3`, e sdist de 347394 bytes, <!-- pragma: allowlist secret -->
  SHA-256 `E790E6EC7AB6F7D7A18B8F6D34FEA5598F52E818D8273DD32DC071F2EEF23594`; <!-- pragma: allowlist secret -->
- smoke externo confirmou `cli`, `metadata_version` e `package_version` em `0.2.0rc1`, fora do
  checkout. Esses artefatos são evidência local; a release final será reconstruída do merge limpo.

## Rollback

- **Antes do merge:** interromper em qualquer aceite negativo, preservar a evidência e reverter por
  commit isolado ou inversão explícita dos hunks a partir de `checkpoint/f7.5-ready`.
- **Depois do merge e antes da tag:** usar revert do merge por PR e repetir a CI; não publicar a RC.
- **Depois da tag/release:** tags publicadas são imutáveis. Não mover nem apagar
  `v0.2.0-rc.1`; marcar a release como superseded se necessário, corrigir em nova branch e publicar
  `0.2.0rc2`/tag nova somente após novo ciclo completo.
- **Verificação:** metadata/package/CLI/SARIF retornam à identidade esperada, docs voltam ao estado
  honesto, árvore fica limpa, checks repetem e refs históricas permanecem intactas.

## Sequência de execução

1. Commitar este congelamento e criar `checkpoint/f7.5-ready` somente local.
2. Implementar versão única, documentação corrente, limitações e testes.
3. Executar aceite focado, matriz, full, F7.3, build/smoke e registrar artefatos.
4. Criar `checkpoint/f7.5-complete`, publicar branch/PR e exigir CI integral.
5. Incorporar somente o head certificado, exigir CI pós-merge e reconciliar a Fase 7.
6. Criar a tag no merge certificado e publicar a GitHub prerelease com artefatos/checksums.

## Checklist de liberação

- [x] baseline Git, predecessoras e CI terminal comprovados;
- [x] escopo de produto, documentação, testes e efeitos remotos congelado;
- [x] rollback definido para pré-merge, pós-merge e tag/release imutável;
- [x] versão `0.2.0rc1` e tag futura `v0.2.0-rc.1` justificadas;
- [x] critérios de aceite e limitações obrigatórias enumerados;
- [x] commit de gate e `checkpoint/f7.5-ready` criados antes da implementação;
- [x] implementação e certificação local concluídas;
- [ ] PR, CI, merge, pós-merge, tag e GitHub prerelease concluídos.

## Próxima ação

Commitar esta certificação, criar `checkpoint/f7.5-complete` somente local e publicar branch/PR sem
force. Tag e release continuam proibidas antes dos merges e CIs exigidos.
