# F3.2 — Configuração e roteamento de modelos

> **Gate:** `READY`
> **Lifecycle:** `ACTIVE`
> **Executor:** `Codex`, único escritor
> **Autorizado em:** `2026-08-07T21:36:30-03:00`
> **Runtime:** `C:\Users\walla\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe` — Python 3.12.13
> **uv:** `.\build\f0.6-tools\uv\bin\uv.exe` — 0.11.32
> **Fronteira:** somente F3.2; tool loop F3.3 e novos efeitos de agente permanecem fora do escopo.

## Problema comprovado

| Evidência reproduzível | Resultado observado |
|---|---|
| inspeção de `ModelRouter.__init__` | recebe `allowed_providers` manual; não existe construtor a partir de `ConfigResolver.resolve()` |
| inspeção de `ProviderRegistry._registry` | catálogo/fábricas são estáticos e ignoram model/base URL/provider configurados efetivamente |
| inspeção de `complete_with_fallback` | captura `RuntimeError`, `TypeError` e `ValueError` indistintamente, repete até auth/invalid/not-implemented e pode fazer fallback após erro não transitório |
| inspeção de `AgentExecutor.execute_node` | concatena o system prompt antes de o router validar a rota/data egress |
| `rg -n "BudgetTracker|complete_with_fallback" src` | nenhum vínculo entre usage real da F3.1 e `BudgetTracker`; só `PolicyEngine.record_usage` usa o tracker isoladamente |
| inspeção de `NodeExecutionResult` e `_append_node_event` | outcome não transporta nem persiste provider, modelo, tokens ou request/response ID efetivamente usados |
| plano principal, tarefa F3.2 | exige configuração efetiva, egress antes do prompt, allowlist, fallback apenas transitório, metadata em node event e budget antes da próxima chamada |

O baseline possui providers HTTP reais da F3.1, mas o caminho de seleção ainda pode repetir falhas
permanentes e não produz evidência retomável da decisão de modelo.

## Baseline conhecido

```yaml
baseline:
  branch: "task/f3.2-model-routing"
  head: "acace943c3acd585cbc83cd047f294c02875b922"
  status: "clean antes da criação do gate; nenhuma mudança preexistente do usuário"
  main: "acace943c3acd585cbc83cd047f294c02875b922"
  origin_main: "acace943c3acd585cbc83cd047f294c02875b922 após git fetch/fast-forward"
  upstream_ci:
    pr_20_run: "31230242232; pull_request; 11/11 completed/success"
    post_merge_run: "31230376744; push; main; head acace943; 11/11 completed/success"
  previous_promotion: "F3.1 arquivada como PROMOTED neste primeiro commit"
  checkpoint: "checkpoint/f3.2-ready no primeiro commit documental desta branch"
  python: "3.12.13"
  uv: "0.11.32"
```

## Contrato técnico congelado

1. A configuração efetiva terá seção `models` explícita: providers nomeados, adapter conhecido,
   modelo, base URL opcional, environment variable opcional para credencial, provider primário e
   fallbacks ordenados. Valores inválidos falham antes de qualquer chamada.
2. `ModelRouter.from_effective_config` lerá somente a configuração resolvida e a allowlist
   `data_egress.allowed_providers`; nenhum default oculto de CLI participará da rota.
3. O registry será instanciável e imutável por router. Só adapters implementados/explicitamente
   registrados poderão ser configurados; provider ausente, duplicado ou fora da allowlist falha fechado.
4. A rota inteira — primário e fallbacks — será validada quanto a registro e data egress antes de
   montar o prompt ou executar transporte. `AgentExecutor` chamará esse preflight antes da concatenação.
5. O router fará no máximo uma invocação por candidato. Retry do mesmo provider permanece no contrato
   F3.1; fallback só ocorrerá quando `ProviderError.retryable` for verdadeiro. Auth, invalid request,
   invalid response, cancelamento e not implemented interrompem sem tentar outro provider.
6. Provider/modelo devolvidos pelo servidor devem corresponder ao provider selecionado e a um modelo
   não vazio. O router nunca reescreve metadata com valores desejados/configurados.
7. Usage real `total_tokens` será registrado no `BudgetTracker` após cada resposta e o budget será
   verificado imediatamente antes de cada chamada/fallback. Budget excedido impede o próximo efeito.
8. `BudgetTracker` ganhará erro tipado, validação de contagens e consulta explícita de disponibilidade,
   preservando `PolicyEngine.record_usage` e falha fechada.
9. `NodeExecutionResult` poderá carregar `ModelCallMetadata` estrita derivada de `LLMResponse`.
   `GraphExecutor` persistirá provider, modelo, usage, request ID e response ID somente no outcome
   `NODE_COMPLETED`/`NODE_FAILED`, quando o provider real já é conhecido; `NODE_STARTED` não antecipará fato.
10. Resume/replay aceitará metadata ausente para nodes determinísticos e históricos, mas validará
    forma, tipos e consistência completa quando metadata de modelo estiver presente.
11. F3.2 não cria backend de agente padrão, não interpreta conteúdo do modelo como output do node e
    não executa tools. Backends explicitamente injetados continuam responsáveis por produzir o
    `NodeExecutionResult`; F3.3 implementará o loop de tool calls.
12. Nenhum segredo, prompt, conteúdo de resposta ou tool payload será persistido nos node events.

## Escopo congelado

### Permitido

- `TASK.md`, `README.md`, `docs/tasks/README.md`, `docs/tasks/completed/F3.1.md` e este dossiê;
- `src/ai_engineering_harness/core/config.py` — defaults e validação da seção efetiva de modelos;
- `src/ai_engineering_harness/defaults/profiles/default.yaml` — configuração default sem segredo;
- `src/ai_engineering_harness/models/registry.py` — registry configurado e fábricas autorizadas;
- `src/ai_engineering_harness/models/router.py` — preflight, fallback transitório e budget;
- `src/ai_engineering_harness/models/__init__.py` — exports F3.2;
- `src/ai_engineering_harness/governance/budget.py` e `governance/__init__.py` — budget tipado;
- `src/ai_engineering_harness/runtime/agent_executor.py` — egress antes de montar prompt;
- `src/ai_engineering_harness/runtime/node_executors.py` e `runtime/__init__.py` — metadata tipada;
- `src/ai_engineering_harness/runtime/graph_executor.py` — persistência e replay da metadata;
- `tests/unit/test_model_routing.py`, `test_config.py`, `test_agent_centric.py` e
  `test_graph_executor.py` — contratos positivos/negativos e journal.

### Proibido

- qualquer path fora do allowlist, inclusive `.github/`, `pyproject.toml`, `uv.lock` e compiler;
- mudar transporte/parsing HTTP F3.1, salvo import público estritamente necessário;
- tool schemas/execução/persistência, `ToolRouter` ou `max_tool_steps` de F3.3;
- path guard, terminal, worktree, promoção ou edição F3.4–F3.8;
- criar backend de agente default, prompt sem preflight, interpretar texto do modelo como output de node;
- persistir prompt, conteúdo, segredo, headers ou argumentos de tools;
- fallback por auth/invalid/cancel/not-implemented, provider fora da allowlist ou candidato duplicado;
- dependência, lockfile, CI, schema de grafo/YAML, política F5 ou fases futuras.

## Critérios de aceite congelados

```yaml
acceptance:
  focused_routing:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_model_routing.py -q
    expected: >-
      configuração efetiva cria registry/rota; egress ocorre antes do prompt; somente erro transitório
      faz fallback; usage real alimenta budget e budget bloqueia antes do próximo transporte
  journal_metadata:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_graph_executor.py -q -k "model_metadata or model_usage"
    expected: >-
      outcome persiste provider/model/usage/IDs reais, started não antecipa e replay rejeita metadata parcial/adulterada
  negative_paths:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_model_routing.py tests/unit/test_agent_centric.py -q -k
      "egress or unregistered or invalid or auth or cancel or budget or fallback"
    expected: "nenhum prompt/transporte/fallback ocorre após falha permanente, rota inválida ou budget esgotado"
  compatibility:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_models.py tests/unit/test_config.py tests/unit/test_agent_centric.py
      tests/unit/test_graph_executor.py tests/e2e/test_retry_context.py -q
    expected: "F3.1, configuração, consumers e ledger/retry F2 permanecem verdes"
  regression:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest tests/unit tests/e2e -q
    expected: "suíte integral verde; somente o skip live F3.1 explicitamente condicionado"
  quality:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m mypy src;
      .\build\f0.6-tools\uv\bin\uv.exe run python -m ruff check .;
      .\build\f0.6-tools\uv\bin\uv.exe run python -m compileall -q src compiler tests;
      .\build\f0.6-tools\uv\bin\uv.exe lock --check;
      git diff --check
    expected: "todos exit 0; lockfile byte-idêntico"
  scope:
    command: >-
      git diff --name-only checkpoint/f3.2-ready;
      git diff --exit-code checkpoint/f3.2-ready -- .github pyproject.toml uv.lock compiler
      src/ai_engineering_harness/contracts src/ai_engineering_harness/persistence
      src/ai_engineering_harness/security src/ai_engineering_harness/tools
      src/ai_engineering_harness/workspace src/ai_engineering_harness/models/provider.py
      src/ai_engineering_harness/models/adapters
    expected: "somente o allowlist; providers F3.1 e fronteiras F3.3–F3.8 byte-idênticos"
```

## Rollback

### Gatilhos

- rota/configuração inválida montar prompt ou causar transporte;
- provider fora da allowlist, não registrado ou duplicado ser executado;
- fallback ocorrer após auth, invalid request/response, cancelamento ou not implemented;
- budget não refletir `total_tokens` real ou permitir efeito depois de esgotado;
- node outcome omitir metadata de uma model call ou journal persistir segredo/conteúdo;
- replay aceitar metadata parcial, inválida ou adulterada;
- algum critério focado, regressão, qualidade, lock ou escopo falhar;
- implementação exigir arquivo, dependência, efeito ou critério fora do congelado.

### Procedimento não destrutivo

Parar e preservar saídas. Antes de commit de implementação, inverter somente hunks F3.2 com
`apply_patch`; depois de commit, usar `git revert` nos commits exclusivos da branch. Nunca usar reset
destrutivo, clean, descarte amplo, force-push ou sobrescrita de trabalho.

### Verificação pós-rollback

Confirmar `git status --short`, HEAD/checkpoint, `git diff --check` e zero diff de produto contra
`checkpoint/f3.2-ready`; repetir focais de routing/journal, suíte integral, mypy, Ruff, compileall e
`uv lock --check`.

## Checklist de liberação do gate

```text
[x] problema comprovado em config, registry, router, prompt, budget e node event
[x] baseline Git, PR #20 e CI pré/pós-merge registrados
[x] promoção F3.1 certificada e arquivada neste primeiro commit
[x] escopo e allowlist exatos; F3.1 transport e F3.3–F3.8 excluídos
[x] configuração efetiva, preflight, fallback transitório, budget e metadata congelados
[x] critérios positivos, negativos, journal/replay, compatibilidade, regressão e qualidade congelados
[x] rollback não destrutivo, gatilhos e verificação executável definidos
[x] executor único, runtime e horário registrados
[x] checkpoint checkpoint/f3.2-ready definido antes de implementação
```

## Evidências de execução

Nenhum arquivo de implementação F3.2 foi alterado. Resultados locais, commits, PR, checks, merge e CI
pós-merge serão registrados somente depois de observados.
