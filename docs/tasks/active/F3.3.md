# F3.3 — Loop de tool calls

> **Gate:** `READY`
> **Lifecycle:** `COMPLETED_LOCAL / PROMOTION_PENDING`
> **Executor:** `Codex`, único escritor
> **Autorizado em:** `2026-08-07T22:08:00-03:00`
> **Runtime:** `C:\Users\walla\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe` — Python 3.12.13
> **uv:** `.\build\f0.6-tools\uv\bin\uv.exe` — 0.11.32
> **Fronteira:** somente F3.3; path guard, terminal seguro, worktree e edição real permanecem F3.4–F3.8.

## Problema comprovado

| Evidência reproduzível | Resultado observado |
|---|---|
| inspeção de `AgentExecutor.execute_node` | executa somente `complete_with_fallback`; `LLMResponse.tool_calls` nunca é consumido |
| inspeção de `ModelRouter` | não existe operação roteada/budgetada para `provider.call_tools` |
| inspeção de `ToolRouter` | recebe allowlist manual, não expõe schemas, não valida JSON Schema e não consome política efetiva compilada do node |
| inspeção de `NodeExecutionResult`/`GraphExecutor` | não há contrato ou eventos duráveis para chamada/resultado de tool |
| `rg -n "max_tool_steps|tool_calls" src` | não existe limite nem loop; apenas parsing dos providers F3.1 |
| plano principal, F3.3 | exige schemas permitidos, validação/autorização, execução pelo router, persistência redigida, retorno ao modelo e condições explícitas de parada |

F3.1 e F3.2 já fornecem tool calls tipadas, roteamento, cancelamento e budget, mas hoje uma resposta
com tool call termina sem efeito, sem continuação e sem evidência. Capabilities declaradas no catálogo
também não equivalem a adapters operacionais e devem continuar fail-closed.

## Baseline conhecido

```yaml
baseline:
  branch: "task/f3.3-tool-loop"
  head: "3956f16fb3046e1eb3721d76f544d6502329cb29"
  status: "clean antes da criação do gate; nenhuma mudança preexistente do usuário"
  main: "3956f16fb3046e1eb3721d76f544d6502329cb29"
  origin_main: "3956f16fb3046e1eb3721d76f544d6502329cb29 após fetch/fast-forward"
  upstream_ci:
    pr_21_run: "31231598253; pull_request; 11/11 completed/success"
    post_merge_run: "31231730863; push; main; head 3956f16; 11/11 completed/success"
  previous_promotion: "F3.2 arquivada como PROMOTED neste primeiro commit"
  checkpoint: "checkpoint/f3.3-ready no primeiro commit documental desta branch"
  python: "3.12.13"
  uv: "0.11.32"
```

## Contrato técnico congelado

1. A política de tools de um agent node será extraída exclusivamente do `CompiledGraphArtifact`, pela
   referência canônica `policies/tool_policy.yaml` e pelo `node_id`. Ausência, duplicidade, formato
   inválido, role divergente ou node sem decisão falha antes de prompt ou efeito.
2. `ToolRouter` terá registros operacionais explícitos contendo nome, descrição, JSON Schema e handler.
   Somente a interseção exata entre registro operacional, allowlist do router e `allowed_tools`
   compiladas poderá ser enviada ao modelo. Capability apenas declarada nunca vira sucesso sintético.
3. Rota completa de modelo, política efetiva, registros e schemas serão validados antes de compor o
   prompt. Nenhum schema de tool proibida/não operacional será enviado ao provider.
4. `ModelRouter` roteará `call_tools` com o mesmo egress, identidade, fallback somente transitório,
   cancelamento e cobrança de `total_tokens` real de F3.2, uma invocação por candidato em cada rodada.
5. A resposta do modelo será final somente quando não contiver tool calls e trouxer conteúdo válido.
   Quando houver calls, o loop executará via `ToolRouter` e enviará ao modelo, na rodada seguinte, um
   transcript canônico contendo call IDs, nomes e resultados JSON-native.
6. Todas as calls de uma resposta serão pré-validadas (nome autorizado, registro operacional, schema,
   call ID único e payload JSON-native) antes do primeiro efeito daquele lote. Uma call inválida impede
   qualquer dispatch do lote.
7. `max_tool_steps` será inteiro positivo e contará tool calls efetivamente despachadas, não rodadas.
   Se um lote ultrapassar o saldo, nenhuma call do lote será executada e `ToolStepLimitExceededError`
   será lançado.
8. Cancelamento e budget esgotado serão verificados antes de cada lote/dispatch e antes da próxima
   chamada de modelo. Cancelamento, tool não autorizada, limite, budget e erro não reparável encerram
   sem nova chamada; F3.3 não inventa retry de tool.
9. Cada dispatch produzirá `ToolExecutionRecord` estrito: step, call ID, nome, digest SHA-256 dos
   argumentos, sucesso/falha, digest do resultado e resumo redigido limitado. Argumentos e resultado
   bruto não serão persistidos; o valor bruto existe somente em memória para retornar ao modelo.
10. `NodeExecutionResult` poderá carregar esses records. `GraphExecutor`, usando o mesmo lock/fencing,
    persistirá pares `TOOL_CALLED` e `TOOL_COMPLETED`/`TOOL_FAILED` antes do outcome do node. Replay
    aceitará ausência histórica, mas rejeitará sequência parcial, adulterada, fora do node/attempt ou
    com campos/tipos inválidos antes de mutação/backend.
11. `AgentExecutor.execute_tool_loop` devolverá um `ToolLoopResult` tipado com resposta final, metadata
    do último model call e records. Não será criado backend de agente default nem texto do modelo será
    convertido automaticamente em output do grafo.
12. F3.3 não torna terminal/Serena/Git seguros nem operacionais para o loop. Adapters determinísticos
    serão injetados somente em testes; adapters reais serão habilitados depois dos gates F3.4–F3.8.

## Escopo congelado

### Permitido

- `TASK.md`, `docs/tasks/README.md`, `docs/tasks/completed/F3.2.md` e este dossiê;
- `src/ai_engineering_harness/models/router.py` — operação `call_tools` roteada;
- `src/ai_engineering_harness/tools/router.py`, `tools/permissions.py` e `tools/__init__.py` — registros,
  schemas, preflight e dispatch sob política compilada, sem novo adapter real;
- `src/ai_engineering_harness/runtime/tool_loop.py` — política efetiva, loop, transcript e records;
- `src/ai_engineering_harness/runtime/agent_executor.py` e `runtime/__init__.py` — entrada tipada;
- `src/ai_engineering_harness/runtime/node_executors.py` — records opcionais no outcome;
- `src/ai_engineering_harness/runtime/graph_executor.py` — eventos e replay de tool;
- `tests/unit/test_tool_loop.py`, `test_model_routing.py`, `test_agent_centric.py` e
  `test_graph_executor.py` — provas positivas, negativas, journal e compatibilidade.

### Proibido

- qualquer path fora do allowlist, inclusive `.github/`, dependências, lockfile, compiler e contratos;
- alterar transporte/parsing dos providers F3.1 ou adapters de modelo;
- path guard/symlink/root confinement F3.4; terminal `argv`/timeout/output F3.5; cancelamento de
  subprocesso F3.6; worktree F3.7; edição/commit F3.8;
- habilitar capability declarada sem registro operacional explícito ou registrar handler fake em produção;
- shell, arquivo, Git, rede/MCP ou qualquer novo efeito real;
- persistir prompt, conteúdo do modelo, argumentos/resultados brutos, segredo ou header;
- retry de tool, aumento silencioso de `max_tool_steps`, fallback permanente ou relaxamento do budget;
- backend padrão, interpretação do conteúdo final como contrato do node ou mudanças de fases futuras.

## Critérios de aceite congelados

```yaml
acceptance:
  focused_loop:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_tool_loop.py -q
    expected: >-
      política compilada e schemas operacionais são preflight; call válida executa, resultado volta ao
      modelo e resposta final encerra com records/usage reais
  negative_paths:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_tool_loop.py tests/unit/test_agent_centric.py -q -k
      "unauthorized or schema or unregistered or duplicate or limit or budget or cancel or tool_error"
    expected: "lote inválido/limite/budget/cancelamento falha antes de efeito ou nova chamada indevida"
  journal_replay:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_graph_executor.py -q -k "tool_event or tool_record"
    expected: "pares redigidos e encadeados; replay aceita ausência e rejeita par parcial/adulterado"
  compatibility:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_models.py tests/unit/test_model_routing.py tests/unit/test_agent_centric.py
      tests/unit/test_graph_executor.py tests/unit/test_policy_registry.py tests/e2e/test_retry_context.py -q
    expected: "F1 policies, F2 ledger/retry e F3.1/F3.2 permanecem verdes"
  regression:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest tests/unit tests/e2e -q
    expected: "suíte integral verde; somente skip live F3.1 explicitamente condicionado"
  quality:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m mypy src;
      .\build\f0.6-tools\uv\bin\uv.exe run python -m ruff check .;
      .\build\f0.6-tools\uv\bin\uv.exe run python -m compileall -q src compiler tests;
      .\build\f0.6-tools\uv\bin\uv.exe lock --check;
      git diff --check
    expected: "todos exit 0; lockfile byte-idêntico"
  scope:
    command: >-
      git diff --name-only checkpoint/f3.3-ready;
      git diff --exit-code checkpoint/f3.3-ready -- .github pyproject.toml uv.lock compiler
      src/ai_engineering_harness/contracts src/ai_engineering_harness/persistence
      src/ai_engineering_harness/security src/ai_engineering_harness/workspace
      src/ai_engineering_harness/models/provider.py src/ai_engineering_harness/models/adapters
      src/ai_engineering_harness/tools/adapters
    expected: "somente o allowlist; fronteiras F3.4–F3.8 e transports byte-idênticos"
```

## Rollback

### Gatilhos

- policy/registro/schema inválido chegar ao prompt ou causar efeito;
- call não autorizada, lote parcial ou step além do limite ser executado;
- fallback permanente, retry de tool, chamada após budget/cancelamento ou metadata fabricada;
- argumento/resultado bruto, prompt, conteúdo ou segredo aparecer no journal;
- tool event parcial/adulterado ser aceito no replay;
- algum gate focado, compatibilidade, regressão, qualidade, lock ou escopo falhar;
- implementação exigir adapter/efeito/path fora do congelado.

### Procedimento não destrutivo

Parar e preservar saídas. Antes de commit, inverter somente hunks F3.3 com `apply_patch`; depois de
commit, usar `git revert` nos commits exclusivos da branch. Nunca reset/clean/force-push ou descarte
amplo. Handlers de teste não entram em produção.

### Verificação pós-rollback

Confirmar status, HEAD/checkpoint, diff-check e zero diff de produto contra `checkpoint/f3.3-ready`;
repetir focais, journal/replay, compatibilidade, suíte integral e todos os quality gates.

## Checklist de liberação do gate

```text
[x] ausência do loop, policy preflight, schemas, limite e eventos comprovada
[x] baseline Git, PR #21 e CI pré/pós-merge registrados
[x] promoção F3.2 certificada e arquivada neste primeiro commit
[x] contrato de policy/registro, lote atômico, loop, budget/cancelamento e parada congelado
[x] persistência redigida e replay completo congelados sem payload bruto
[x] escopo exato; transports e fronteiras F3.4–F3.8 excluídos
[x] critérios positivos, negativos, compatibilidade, regressão, qualidade e escopo congelados
[x] rollback não destrutivo, executor, runtime e checkpoint definidos
```

## Evidências de execução

### Implementação observada

| Área | Resultado |
|---|---|
| Policy preflight | decisão exata do agent node é extraída do artifact compilado; ausência, duplicidade, formato/role divergente falham antes do prompt |
| Registry/schemas | somente registros operacionais explícitos, permitidos simultaneamente pelo router e pela policy compilada, geram schemas para o provider |
| Lote | nomes, call IDs e payloads são validados integralmente por JSON Schema antes do primeiro dispatch |
| Loop | resultados JSON-native retornam ao modelo em transcript canônico; ausência de tool calls com conteúdo final válido encerra |
| Roteamento | `call_tools_with_fallback` preserva egress, fallback transitório, identidade real, cancelamento e budget F3.2 |
| Limites | `max_tool_steps` conta calls; lote acima do saldo, budget esgotado e cancelamento impedem o próximo efeito |
| Falhas | tool não recebe retry implícito; erro para o loop, não chama modelo novamente e carrega record de falha redigido |
| Evidência | argumentos/resultados brutos não entram no journal; cada record contém IDs, nome, digests e resumo redigido limitado |
| Journal/replay | `TOOL_CALLED` e `TOOL_COMPLETED`/`TOOL_FAILED` usam lock/fencing do node; pares parciais/adulterados falham fechado |
| Fronteira futura | nenhum adapter real novo foi habilitado; path guard, terminal, worktree e edição permanecem F3.4–F3.8 |

### Validações executadas

| Critério | Resultado observado |
|---|---|
| Gate documental | commit `42c8b3b`; tag local `checkpoint/f3.3-ready`; implementação começou somente após `READY` |
| Focado loop | `12 passed` em `tests/unit/test_tool_loop.py` |
| Negativos | `10 passed, 14 deselected`; unauthorized, schema, unregistered, duplicate, limit, budget, cancel e tool error |
| Journal/replay | `4 passed, 32 deselected`; pares completos, par parcial e evento adulterado |
| Compatibilidade | `120 passed` em policies F1, ledger/retry F2 e modelos/roteamento F3.1/F3.2 |
| Suíte integral | `508 passed, 1 skipped, 6 subtests passed`; único skip live F3.1 condicionado |
| mypy | sem issues em 99 arquivos; somente duas notas preexistentes do indexer |
| Ruff | todos os checks verdes |
| compileall | exit 0 para `src`, `compiler` e `tests` |
| Lock | `uv lock --check` exit 0; 41 packages; lock byte-idêntico |
| Diff | `git diff --check` exit 0 |
| Escopo | somente allowlist F3.3; contracts, persistence, security, workspace, transports e tool adapters byte-idênticos |

O primeiro passe Ruff identificou somente uma variável de teste não utilizada; ela foi removida e o
gate repetido ficou verde. Nenhum critério, teste ou configuração foi removido, ignorado ou enfraquecido.

### Commits e checkpoint local

| Commit | Conteúdo |
|---|---|
| `42c8b3b` | certificação da promoção F3.2 e gate F3.3 completo antes da implementação |
| `11d88ee` | registry/schema, loop, roteamento de call_tools, records, journal/replay e testes |

O fechamento documental receberá a tag local `checkpoint/f3.3-complete`. PR, checks, merge e CI
pós-merge só serão registrados depois de observados; a promoção será certificada no gate F3.4.

## Checklist de conclusão local

```text
[x] policy compilada, registros e schemas validados antes de prompt/efeito
[x] lote inteiro validado antes do primeiro dispatch
[x] resultado retornado ao modelo e resposta final válida encerra
[x] fallback apenas transitório e usage real cobrado em cada model call
[x] limite, budget, cancelamento, unauthorized e erro param sem efeito extra
[x] tool records não persistem argumentos/resultados brutos
[x] pares de eventos usam lock/fencing e replay rejeita parcial/adulterado
[x] 508 testes + 6 subtests verdes; único skip live condicionado
[x] mypy, Ruff, compileall, lock, diff-check e escopo verdes
[x] F3.4–F3.8 e adapters reais permanecem fora do diff
[ ] PR único publicado e todos os checks explicitamente inspecionados
[ ] merge e CI push em main certificados no primeiro commit do gate F3.4
```
