# F3.C2 — Execução durável de tools e policy

> **Gate:** `READY`
> **Lifecycle:** `ACTIVE`
> **Executor:** `Codex`, único escritor
> **Autorizado em:** `2026-08-08T02:02:32-03:00`
> **Runtime:** `C:\Users\walla\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe` — Python 3.12.13
> **uv:** `.\build\f0.6-tools\uv\bin\uv.exe` — 0.11.32
> **Fronteira:** somente F3.C2; path guard, terminal, worktree, promoção e edição F3.4–F3.8 permanecem bloqueados.

## Problema comprovado

| Evidência reproduzível | Resultado observado |
|---|---|
| `rg -n "result = .*_execute_node|_append_tool_events" src/ai_engineering_harness/runtime/graph_executor.py` | o backend termina antes de `_append_tool_events`; `TOOL_CALLED` não é write-ahead |
| inspeção de `ToolLoopExecutor.execute` | `ToolRouter.dispatch` executa o handler sem um recorder durável ligado ao lock/fencing do node |
| interrupção entre handler e `_append_tool_events` | o efeito pode ocorrer sem chamada correspondente no journal; retomada não consegue distinguir efeito ausente de efeito ambíguo |
| `uv run python -c "... ToolRouter(['serena_edit']).registered_tools"` | ausência de `registrations` produziu `('serena_edit', 'terminal_run')`, em vez de registry operacional vazio |
| inspeção de `EffectiveToolPolicy` | `denied_tools` é extraído, mas não é enviado a `prepare`, `validate_calls` ou `dispatch`; `human_approval_required` nem integra o contrato runtime |
| inspeção de `AgentExecutor.execute_tool` | existe bypass que chama `ToolRouter.dispatch` diretamente, sem write-ahead, policy compilada, budget ou cancelamento |

Essas lacunas permitem efeito sem evidência prévia, sucesso operacional implícito e dispatch que não
preserva toda a decisão compilada. F3.4 não pode habilitar paths reais sobre essa base.

## Baseline conhecido

```yaml
baseline:
  branch: "task/f3.c2-tool-effect-durability"
  head: "5616fc548716acb3561dd67d3905eb008130b58c"
  status: "clean; nenhuma mudança preexistente do usuário"
  main: "5616fc548716acb3561dd67d3905eb008130b58c"
  origin_main: "5616fc548716acb3561dd67d3905eb008130b58c"
  upstream_ci:
    pr_23_run: "31240274131; pull_request; head 78a0099; 11/11 completed/success"
    post_merge_run: "31240455344; push; main; head 5616fc5; 11/11 completed/success"
  previous_promotion: "F3.C1 certificada e arquivada neste primeiro commit"
  checkpoint: "checkpoint/f3.c2-ready aponta para o primeiro commit documental deste gate"
  python: "3.12.13"
  uv: "0.11.32"
```

## Contrato técnico congelado

1. Cada tool call recebe uma intenção tipada contendo step, call ID, nome e digest dos argumentos.
   O runtime grava `TOOL_CALLED` antes do handler usando o mesmo lock e fencing token do node.
2. `TOOL_COMPLETED` ou `TOOL_FAILED` só é gravado depois que o handler retorna ou falha. Argumentos e
   resultados brutos nunca entram no journal; apenas digests, resultado redigido limitado e código seguro.
3. O recorder durável é fornecido pelo `GraphExecutor` ao backend dentro de `NodeExecutionContext` e
   valida sequência, identidade e correspondência exata com `NodeExecutionResult.tool_executions`.
4. Falha ao gravar `TOOL_CALLED` impede o dispatch. Falha/crash depois da chamada e antes do outcome
   deixa chamada aberta; retomada falha como `requires_intervention` e nunca reexecuta automaticamente.
5. Outcome de tool completo seguido de falha no outcome/CAS do node também nunca causa reexecução do
   backend; o replay histórico de pares completos F3.C1 continua aceito.
6. Tool loop sem recorder durável falha antes do primeiro efeito. O bypass `AgentExecutor.execute_tool`
   deixa de executar efeitos fora do loop durável.
7. `registrations=None` significa registry operacional vazio. Handlers determinísticos existem somente
   em testes; capability declarada não cria adapter ou sucesso sintético em produção.
8. Deny vence allow no ponto de prepare/validação/dispatch. Duplicidade ou overlap entre allow/deny
   falha antes do model call e de qualquer efeito.
9. `human_approval_required=true` é preservado no contrato efetivo e falha fechado antes do model call
   e do dispatch enquanto F5.6 não fornecer aprovação vinculada ao conteúdo.
10. Cancelamento e budget são reavaliados para cada call imediatamente antes do write-ahead/dispatch;
    lote inteiro continua validado antes do primeiro efeito.

## Escopo congelado

### Permitido

- `TASK.md`, `docs/tasks/README.md`, `docs/tasks/completed/F3.C1.md` e este dossiê — promoção, gate e evidência;
- `src/ai_engineering_harness/runtime/tool_loop.py` — policy efetiva, recorder obrigatório e ordem do dispatch;
- `src/ai_engineering_harness/runtime/agent_executor.py` — propagação do recorder e bloqueio do bypass;
- `src/ai_engineering_harness/runtime/node_executors.py` — intenção/recorder tipados no contexto do node;
- `src/ai_engineering_harness/runtime/graph_executor.py` — write-ahead/outcome sob lock, fencing e replay fail-closed;
- `src/ai_engineering_harness/runtime/__init__.py` — exports públicos dos contratos F3.C2;
- `src/ai_engineering_harness/tools/router.py` — registry vazio e deny-wins no dispatch;
- `src/ai_engineering_harness/tools/__init__.py` — exports de erros runtime quando necessários;
- `tests/unit/test_tool_loop.py`, `test_graph_executor.py`, `test_agent_centric.py`, `test_phase4.py`,
  `test_task_ledger.py` e `test_public_module_imports.py` — provas focadas, compatibilidade e documentação.

### Proibido

- qualquer dependência, workflow, schema/versionamento de artefato ou mudança em `.github`, `pyproject.toml`, `uv.lock` ou `compiler/`;
- alterar persistence/storage, contracts serializados, provider/model routing, security, workspace ou adapters de tool;
- implementar path guard, terminal `argv`, processo filho, worktree Git, promoção, edição ou MCP de F3.4–F3.8;
- criar handler/default backend de produção, aprovação sintética, retry automático de tool ou sucesso fabricado;
- persistir argumentos/resultados brutos, prompt, conteúdo do modelo, headers, secrets ou exceção crua;
- iniciar F3.4 ou modificar seus critérios nesta branch/PR.

## Critérios de aceite congelados

```yaml
acceptance:
  durable_dispatch:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_graph_executor.py tests/unit/test_tool_loop.py -q -k
      "write_ahead or durable or ambiguous or crash or recorder or tool_event"
    expected: >-
      chamada precede handler; outcome sucede efeito; falhas nos quatro limites não reexecutam
      efeito e chamada aberta bloqueia resume
  policy_dispatch:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_tool_loop.py tests/unit/test_agent_centric.py tests/unit/test_phase4.py -q -k
      "approval or deny or overlap or registration or bypass or cancel or budget"
    expected: >-
      deny-wins, overlap e aprovação falham antes de model/effect; registry ausente é vazio;
      budget/cancelamento são verificados por dispatch
  compatibility:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_policy_registry.py tests/unit/test_tool_loop.py tests/unit/test_graph_executor.py
      tests/unit/test_agent_centric.py tests/e2e/test_execution_resume.py -q
    expected: "compilação de policy, F3.C1, replay histórico e retomada F2 permanecem verdes"
  documentation:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_task_ledger.py tests/unit/test_documentation.py tests/unit/test_encoding.py -q
    expected: "painel curto, F3.C1 arquivada, um dossiê ativo, links e UTF-8 íntegros"
  regression:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest tests/unit tests/e2e -q
    expected: "suíte integral verde; somente integração live explicitamente condicionada pode skipar"
  quality:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m mypy src;
      .\build\f0.6-tools\uv\bin\uv.exe run python -m ruff check .;
      .\build\f0.6-tools\uv\bin\uv.exe run python -m compileall -q src compiler tests;
      .\build\f0.6-tools\uv\bin\uv.exe lock --check;
      git diff --check
    expected: "todos exit 0; lockfile byte-idêntico"
  package:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m build;
      .\build\f0.6-tools\uv\bin\uv.exe run python tests/ci/smoke_wheel.py
    expected: "sdist/wheel gerados e smoke isolado confirma pacote, metadata e CLI"
  scope:
    command: >-
      git diff --name-only checkpoint/f3.c2-ready;
      git diff --exit-code checkpoint/f3.c2-ready -- .github pyproject.toml uv.lock compiler
      src/ai_engineering_harness/contracts src/ai_engineering_harness/persistence
      src/ai_engineering_harness/models src/ai_engineering_harness/security
      src/ai_engineering_harness/workspace src/ai_engineering_harness/tools/adapters
    expected: "somente allowlist F3.C2; F3.4–F3.8 e fronteiras excluídas byte-idênticas"
```

Falha em qualquer critério permanece bloqueante. Teste novo pode aumentar evidência, nunca substituir,
remover ou enfraquecer o congelado.

## Rollback

### Gatilhos

- handler executar antes de `TOOL_CALLED` durável ou outcome aparecer antes do efeito;
- chamada aberta ser reexecutada automaticamente ou replay aceitar par divergente/adulterado;
- recorder não usar lock/fencing do node ou resultado do backend divergir do journal sem falhar;
- capability declarada criar registration/handler implícito em produção;
- deny/overlap/aprovação alcançar model call ou handler; budget/cancelamento não bloquear dispatch;
- payload bruto, prompt, conteúdo, secret ou exceção crua entrar no journal;
- compatibilidade, documentação, regressão, quality, package ou escopo falhar;
- solução exigir path, processo, Git, dependência ou arquivo fora do congelado.

### Procedimento não destrutivo

Parar e preservar saídas. Antes de commit, inverter somente hunks F3.C2 com `apply_patch`. Depois de
commit, usar `git revert` nos commits exclusivos desta branch. Nunca usar reset/clean/force-push nem
descartar trabalho alheio. Descoberta fora de escopo muda o gate para `BLOCKED` e exige recongelamento.

### Verificação pós-rollback

Confirmar status, HEAD/checkpoint, `git diff --check` e zero diff de produto contra
`checkpoint/f3.c2-ready`; repetir documentação, compatibilidade, regressão, quality e package.

## Checklist de liberação

```text
[x] problema comprovado por inspeção e reprodução das registrations implícitas
[x] baseline Git, PR #23, merge e CI pré/pós-merge registrados
[x] promoção F3.C1 certificada e arquivada neste primeiro commit
[x] escopo permitido e proibido congelado antes da implementação
[x] write-ahead, outcomes, crash/recovery e correspondência backend/journal congelados
[x] deny-wins, overlap, aprovação, budget, cancelamento e registry vazio congelados
[x] F3.4–F3.8, dependências, schemas e adapters explicitamente excluídos
[x] critérios focados, compatibilidade, documentação, regressão, quality, package e escopo congelados
[x] rollback não destrutivo, executor, runtime, horário e checkpoint definidos
```

## Evidências de execução

Ainda não iniciada. Nenhum arquivo de implementação F3.C2 foi alterado antes deste gate `READY`.
