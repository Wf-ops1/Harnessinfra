# F7.C1 — Composição operacional do caminho público

> **Gate:** `COMPLETED_LOCAL`
> **Lifecycle:** `PROMOTED`
> **Autorização vigente:** promoção F7.C1 concluída; reconciliação administrativa e ciclo integral F7.5 autorizados, sem remoção de refs
> **Branch:** `task/f7.c1-public-path-composition`
> **Branch remota:** `origin/task/f7.c1-public-path-composition`, preservada no head final `bb4dc8c62d60699478c2ccbb37c5d2fbcba21f55` <!-- pragma: allowlist secret -->
> **PR de produto:** [#92](https://github.com/Wf-ops1/Hartrol/pull/92), head final `bb4dc8c62d60699478c2ccbb37c5d2fbcba21f55` <!-- pragma: allowlist secret -->
> **CI final do PR:** [33325055342](https://github.com/Wf-ops1/Hartrol/actions/runs/33325055342), 11/11 jobs obrigatórios mais `CI required` verdes
> **Merge de produto:** `26c36ffaa5b3edde616066a15c1db7fe4b321d12` em `2026-08-30T17:35:20Z` <!-- pragma: allowlist secret -->
> **CI pós-merge:** [33325679613](https://github.com/Wf-ops1/Hartrol/actions/runs/33325679613), 11/11 jobs obrigatórios mais `CI required` verdes no merge exato
> **Tags remotas:** nenhuma
> **Reconciliação administrativa:** `LOCAL_READY / PUBLICATION_PENDING` em `docs/promote-f7.c1`, sem upstream
> **Baseline Git:** `0ea7f801f568b07c31d1f94c7ad9b4cea40d3330` (`main == origin/main`)
> **Checkpoint READY:** `checkpoint/f7.c1-ready` → `caf1dad592dbade2feb84bc5c1eb65037b3b3db1`, somente local
> **Produto local:** `2415f41aff0d746dc11d58a662d8998de6181b76`
> **Checkpoint COMPLETE:** `checkpoint/f7.c1-complete` → `f1d8cd0bc9babed816065000e92972e86dcc3c00`, somente local
> **Executor:** `Codex`, único escritor autorizado
> **Autorizado em:** `2026-08-30T02:11:04-03:00`
> **Produto promovido em:** `2026-08-30T17:35:20Z`
> **Reconciliação preparada em:** `2026-08-30T14:51:00-03:00`
> **Runtime:** `C:\Users\walla\OneDrive\Desktop\ai-engineering-harness\.venv\Scripts\python.exe` — Python 3.12.13

## Dependência terminal comprovada

A F7.4 encerrou produto e reconciliação antes desta branch. O PR de produto #90 foi incorporado no
merge `a62c16457bbd04cc9c30e31df65d429e30b2158f`; a reconciliação administrativa #91 encerrou no
head `f4b4f45b9ecc1e1e8ce950077d19c1613cf5fad6`, CI `33293246380` e merge
`0ea7f801f568b07c31d1f94c7ad9b4cea40d3330`. A consulta read-only do run de `push`
`33293579533` confirmou `success` no SHA exato, com onze jobs obrigatórios mais `CI required`.
Depois de `git fetch origin main`, `HEAD == origin/main == 0ea7f801...` e a árvore estava limpa.

## Problema comprovado

O caminho público instalado ainda não seleciona uma composição operacional. O probe isolado abaixo
inicializou o scaffold real e executou o comando público sem monkeypatch:

```text
init_exit=0
run_exit=1
Error: agent node executor backend is unavailable
```

O teste baseline correspondente passou `1 passed in 1.20s` porque
`tests/unit/test_cli_runtime.py::test_cli_run_status_inspect_lifecycle` exige justamente essa falha.
A causa direta está em `src/ai_engineering_harness/cli/main.py`: `_lifecycle_service()` injeta
`NodeExecutorRegistry()` vazio. A prova F7.1 em `tests/e2e/test_external_repository_product.py`
atravessa o produto somente após construir `InstalledWheelAgentBackend`, provider, tool router,
worktree, promotion e rollback dentro de uma fixture. Logo, a wheel contém as primitivas, mas a
factory pública não as compõe.

A auditoria revelou uma segunda lacuna estrutural vinculada: o grafo default `new-feature` declara
roles e tools conceituais que não correspondem diretamente aos adapters operacionais registrados.
O gate deve alinhar o workflow estável com contratos e capabilities executáveis; não basta preencher
um backend que continuaria falhando no primeiro encadeamento ou grant ausente.

## Baseline conhecido

- branch de origem `main`, limpa, sem mudanças preexistentes;
- `HEAD == origin/main == 0ea7f801f568b07c31d1f94c7ad9b4cea40d3330` antes da branch;
- CI pós-reconciliação `33293579533` verde no SHA exato, 11 jobs mais `CI required`;
- branch exclusiva local, sem upstream e sem qualquer ref remota criada;
- Python 3.12.13 na `.venv` registrada acima;
- reprodução pública falha antes de criar execution bundle por backend ausente;
- teste baseline focado verde porque preserva a falha fechada promovida em F2.5;
- dossiê F7.4 concluído permanece imutável; a evidência externa posterior é autoritativa.

## Contrato técnico congelado

1. Uma factory pública canônica construirá, para `new-feature`, configuração, provider router,
   tools confinadas, worktree externo, contexto, planning, executores, verification, approval,
   promotion, knowledge, evidence, cancellation e rollback usando somente componentes de produção.
2. `harness run new-feature` selecionará essa factory; workflows sem composição registrada
   continuarão falhando fechados, sem fallback genérico ou sucesso simulado.
3. A execução criará/recarregará o worktree pelo mesmo `execution_id`; escrita de agente, command e
   knowledge ficará confinada ao worktree, nunca ao checkout original antes da promoção aprovada.
4. Provider, credencial, trust, policy, grant, adapter ou aprovação ausente/incompatível falhará antes
   do efeito correspondente. Nenhum arquivo do projeto, marker ou resposta de modelo fabricará
   autoridade humana.
5. Resume e comandos subsequentes usarão artifact, configuração redigida, trust snapshot, identidades
   Git e worktree duráveis da mesma execução. Divergência bloqueará em vez de recompor silenciosamente.
6. Candidate, gates, approval vinculada ao conteúdo, promotion, knowledge transaction, evidence,
   cancel e rollback continuarão sob os serviços canônicos já promovidos; a F7.C1 fará wiring e as
   adaptações mínimas de fronteira, sem criar protocolos paralelos.
7. O E2E construirá e instalará a wheel em ambiente isolado, operará sobre repositório Git externo
   descartável e invocará CLI/factory pública. O modelo controlado usará um adapter HTTP de produção
   contra transporte local controlado; doubles permanecerão somente sob `tests/`.
8. Capabilities live que dependem de serviço, credencial ou Serena externo permanecerão opt-in e
   documentadas como limitações, sem registro simulado de produção.

## Escopo congelado

### Implementação permitida

- `src/ai_engineering_harness/runtime/`: nova composição/backends públicos e ajustes mínimos no
  lifecycle/facade para preservar worktree, configuração e identidades entre comandos;
- `src/ai_engineering_harness/cli/main.py`: selecionar a composição `new-feature`, expor somente
  grants/intenções explícitos necessários e manter erros redigidos/fail-closed;
- `src/ai_engineering_harness/contracts/nodes/` e exports relacionados: somente um contrato de estado
  operacional do workflow se a compatibilidade entre nós exigir;
- defaults `graphs/new-feature.yaml`, `policies/tool_policy.yaml`, `tools/tool_registry.yaml`, agents
  e profile somente para alinhar o workflow público às capabilities reais e default-deny;
- `src/ai_engineering_harness/tools/`, `models/`, `knowledge/` e `workspace/` somente para factories
  estreitas ou integração comprovadamente ausente; adapters/protocolos promovidos devem ser reusados;
- testes unitários, integração/E2E, runner F7.2 e smoke de wheel estritamente necessários à composição.

### Estado e documentação permitidos

- `README.md`, `TASK.md`, `docs/tasks/README.md`, este dossiê e `docs/user_guide.md`;
- `tests/unit/test_documentation.py` e `tests/unit/test_task_ledger.py` para o estado auditável;
- `.secrets.baseline` somente se deslocamentos mecânicos de linhas forem inevitáveis, sem alterar
  detector, hash, classificação ou aceitar segredo novo.

### Reuso obrigatório sem enfraquecimento

- `ConfigResolver`, `ModelRouter`/`ProviderRegistry`, adapters HTTP reais, `PolicyRegistry`,
  `build_operational_tool_router`, `ExternalWorktreeManager`, `ExecutionLifecycleService`,
  `PromotionManager`, `KnowledgeTransactionManager`, evidence/audit, cancellation e rollback;
- bundle/journal/FSM, approval ligada ao conteúdo, path guard, budgets e redaction;
- matriz F7.2, gates F7.3, smoke F7.4 e CI Windows/Linux.

### Fora de escopo e proibido

- novo provider simulado, factory específica da fixture ou mock registrado sob `src/`;
- implícita autorização de promotion, segredo, Python project contract, hook ou tool destrutiva;
- alterar schema persistido, taxonomia de gates, thresholds F7.3, proteção de branch ou `CI required`;
- nova dependência ou mudança em `uv.lock`, salvo evidência nova que reabra formalmente o gate;
- expandir composição pública para `bug-fix`, `refactoring`, `migration` ou `incident`;
- bump de versão, release, tag, merge ou remoção de refs; push/PR somente sob autorização nominal posterior.

## Critérios de aceite congelados

1. O probe público deixa de falhar por registry vazio e os negativos unitários provam falha fechada
   separada para provider/configuração, credencial, trust, policy/tool grant e promoção sem aprovação.
2. Testes focados novos comprovam factory canônica, worktree lazy/idempotente, tools confinadas,
   resume com a mesma configuração/identidades e ausência de escrita no checkout original.
3. Um E2E de wheel instalada em repositório Git externo executa o caminho público `new-feature`,
   cria candidate, executa gates reais, solicita/aplica approval, promove, sincroniza knowledge,
   verifica evidence/audit e executa rollback na mesma execução; nenhuma lifecycle factory da fixture.
4. O E2E usa adapter de produção contra transporte controlado e prova que o pacote importado vem do
   ambiente instalado, não do checkout fonte.
5. `.venv\Scripts\python.exe -m pytest` focado da F7.C1, `tests/run_f7_2_matrix.py`, suíte integral,
   testes Windows/Linux aplicáveis e smoke de wheel passam sem relaxamento.
6. Ruff, `mypy --strict src`, compileall, build/smoke, coverage/branches, secret scan, dependency audit,
   `git diff --check` e auditoria do allowlist ficam verdes.
7. A CI do eventual PR passa todos os jobs obrigatórios e `CI required`; publicação dependerá de
   autorização posterior e não integra o gate local atual.

## Implementação entregue

- a factory pública `build_new_feature_lifecycle` compõe storage, configuração, router de provider,
  tool loop, worktree externo, knowledge transaction, verification, approval, promotion e rollback;
- o estado estrito `NewFeatureExecutionState` e o grafo default `3.3.0` ligam
  `implement_feature → knowledge_sync_update → END` às capabilities operacionais reais;
- CLI pública cria input canônico, exige grants explícitos, recompõe comandos posteriores pela
  identidade persistida e expõe candidate, pedido de promoção e promote sem autoridade implícita;
- o backend recebe base/configuração do bundle sob o lock vigente, confina tools ao worktree e
  reconhece arquivos rastreados modificados e arquivos novos não rastreados;
- Git gerenciado usa `core.longpaths=true`, eliminando a falha Windows observada nos snapshots de
  knowledge sem reduzir nomes, evidência ou isolamento.

## Certificação local

- produto local `2415f41aff0d746dc11d58a662d8998de6181b76`; no momento da certificação, allowlist
  auditado, sem alteração de dependências, schema persistido, `uv.lock`, versão ou refs remotas;
- E2E da wheel instalada pelo CLI público: `1 passed in 41.89s`, cobrindo provider HTTP de produção,
  tools, knowledge, candidate, três gates reais, approval, promotion, evidence/audit, rollback e cleanup;
- focado final das novas fronteiras: `106 passed in 44.73s`; matriz F7.2:
  `62 passed in 74.16s`; full final: `1106 passed, 5 skipped, 6 subtests passed in 471.47s`;
- coverage exata do produto: `1106 passed, 5 skipped, 6 subtests passed in 555.76s`, core crítico
  `88.73%`, seis arquivos, 23 funções decisórias e zero branch exigido ausente;
- Ruff, mypy strict em 112 arquivos-fonte, compileall, `uv lock --check`, `git diff --check`, build e
  smoke oficial verdes; wheel 0.1.0 com 392543 bytes e SHA-256
  `D907BC6E17B22B73C90575269AE7354A186B950038153E81CC48A5EB18D613B0`;
- dependency audit: 74 dependências, zero ignorada e zero vulnerabilidade; a baseline preserva os
  mesmos 307 hashes revisados e o scan diferencial dos 23 arquivos alterados encontrou zero achado.
  O wrapper Windows continua convertendo separadores ao tentar o scan integral; paths foram
  normalizados, identidades preservadas e a CI Ubuntu permanece autoridade desse gate de promoção.

## PR #92 e reparos R1–R3

- a autorização nominal publicou somente os três commits certificados, sem `force` e sem tags, e
  abriu o PR #92 no head `f1d8cd0bc9babed816065000e92972e86dcc3c00` contra `main`;
- o run inicial [33321887150](https://github.com/Wf-ops1/Hartrol/actions/runs/33321887150)
  aprovou quality nas quatro combinações, package em Ubuntu/Windows e tests em Ubuntu 3.11/3.14;
- security/coverage aprovou `1109 passed, 2 skipped, 6 subtests`, core `88.73%`, 23 decisões e zero
  branch ausente, mas o scan integral Linux encontrou um falso positivo `Hex High Entropy String` no
  SHA documental de `tests/unit/test_task_ledger.py:1109` ainda sem a pragma usada pelos SHAs vizinhos;
- tests Windows 3.11/3.14 chegaram a `42 passed, 1 skipped` e falharam somente no novo E2E porque o
  teste isolava `LOCALAPPDATA` antes de `uv build --offline`, ocultando o cache preparado por `uv sync`;
- o reparo mínimo marca somente o SHA documental como fixture e adia o `LOCALAPPDATA` isolado até
  depois da build/instalação; o E2E Windows no caminho corrigido passou `1 passed in 103.78s` e o
  ledger passou `21 passed`;
- a baseline `.secrets.baseline` permanece byte a byte no HEAD: a tentativa do hook Windows de trocar
  `/` por `\` foi descartada integralmente, sem aceitar, reclassificar ou alterar qualquer finding.
- o run R1 [33323254683](https://github.com/Wf-ops1/Hartrol/actions/runs/33323254683) confirmou
  ambos os E2E Windows e todos os outros nove jobs; o scan encontrou zero segredo novo, mas retornou
  código 3 porque quatro asserções documentais redundantes deslocavam linhas de fixtures revisadas;
- o reparo R2 removeu quatro asserções redundantes do R1 e preservou todas as verificações anteriores,
  mas o run [33324024275](https://github.com/Wf-ops1/Hartrol/actions/runs/33324024275) mostrou que quatro
  asserts legítimos da certificação original ainda antecediam fixtures históricas: todos os dez outros
  jobs passaram, inclusive Windows 3.11/3.14, e security aprovou `1109/2/6`, core `88.73%`, 23 decisões
  e zero branch ausente antes de o hook retornar código 3;
- o R3 reposiciona a asserção documental e as três do ledger depois da última fixture revisada nos
  mesmos testes, sem remover cobertura, alterar conteúdo auditado ou tocar `.secrets.baseline`;
- a reprodução isolada exata do gate Windows passou `files=346 reviewed_fixtures=307 new_secrets=0`;
  documentação passou `36/36`, Ruff, compileall e `git diff --check` também ficaram verdes.

## Certificação final de promoção

O R3 foi consolidado no head final `bb4dc8c62d60699478c2ccbb37c5d2fbcba21f55` e publicado sem
force-push. O run [33325055342](https://github.com/Wf-ops1/Hartrol/actions/runs/33325055342)
passou os onze jobs obrigatórios mais `CI required`, inclusive security/coverage e os E2E Windows
3.11/3.14. O PR #92 foi incorporado por merge commit em `26c36ffaa5b3edde616066a15c1db7fe4b321d12`
às `2026-08-30T17:35:20Z`. A CI de `push`
[33325679613](https://github.com/Wf-ops1/Hartrol/actions/runs/33325679613) repetiu os onze jobs mais
`CI required` com sucesso no SHA exato. A branch de produto foi preservada, nenhuma tag foi publicada
e as falhas intermediárias continuam registradas acima. Portanto, a F7.C1 está historicamente
`PROMOTED`; a F7.5 aguarda somente esta reconciliação administrativa terminal.

## Checklist de liberação

- [x] baseline Git, predecessor, runtime e CI anterior observados;
- [x] problema reproduzido pelo caminho público sem alterar produto;
- [x] escopo, critérios, dependências e efeitos esperados congelados;
- [x] rollback, responsabilidade, autorização e verdade temporal registrados;
- [x] checkpoint local `checkpoint/f7.c1-ready` criado antes do primeiro arquivo de produto;
- [x] implementação permanece no allowlist e todos os negativos originais continuam auditáveis;
- [x] aceite local integral certificado em um único head candidato;
- [x] checkpoint local complete criado sem publicar refs;
- [x] branch e PR #92 criados somente após autorização específica; nenhuma tag foi publicada;
- [x] run negativo `33321887150` preservado e gate reaberto como `REPAIR_ACTIVE / PROMOTION_BLOCKED`;
- [x] R3 local preserva a baseline, passa secrets `346/307/0`, documentação `36/36`, Ruff e compileall;
- [x] R3 publicado, PR #92 integralmente verde, merge `26c36ff` e CI pós-merge `33325679613` certificados.

## Reconciliação administrativa pós-merge — DEC-014

> **Estado:** `LOCAL_READY / PUBLICATION_PENDING`

A branch `docs/promote-f7.c1` parte de
`main == origin/main == 26c36ffaa5b3edde616066a15c1db7fe4b321d12` depois da CI pós-merge
verde. Esta reconciliação arquiva o dossiê e alinha painel, índice, README e testes de estado; não
altera produto, dependências, schemas, defaults ou workflow. O usuário autorizou o ciclo integral da
F7.5, incluindo publicação e merge desta dependência administrativa, mas a implementação F7.5 só
começará depois do PR documental e da respectiva CI em `main` ficarem verdes.

## Rollback

- **Gatilhos:** escrita fora do worktree; authority implícita; resposta simulada; divergência de
  bundle/trust/Git aceita; caminho instalado dependendo do checkout; gate F7.2/F7.3/F7.4 negativo;
  schema persistido ou dependência alterado sem recongelamento.
- **Procedimento:** interromper e marcar `REPAIR_ACTIVE / PROMOTION_BLOCKED`; preservar a evidência e
  reverter por commit isolado ou inversão explícita dos hunks F7.C1 a partir de
  `checkpoint/f7.c1-ready`. Nunca usar reset destrutivo nem apagar trabalho preexistente.
- **Verificação:** repetir probe negativo original, testes focados, matriz F7.2, full/gates F7.3,
  build/smoke e confirmar branch/base/status e ausência de refs remotas novas.

## Próxima ação

Publicar a reconciliação em um PR documental, exigir CI integralmente verde e incorporá-la por merge
commit. Não iniciar F7.5 antes da CI pós-merge, não publicar tag nesta branch e não remover refs.
